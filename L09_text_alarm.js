let Xpos;
let speed;

function setup() {
    createCanvas(500,500);
    texxtAlign(CENTER, CENTER);
    textSize(48);
    xpos = width/2
    speed = 2;
}

function draw(){
    background("lightgray");
    Text()
}