function setup(){ 
    createCanvas(600, 600);
}       

function draw() {
    background("black");
    fill("yellow");

    textSize(38);
    let ypos = 50;
    let foodList = ["burger, tacos, ice cream, "]
}